{
  "name": "employee-punch-frontend",
  "version": "1.0.0",
  "description": "Employee Punch In/Out Tracking System - Frontend",
  "main": "login.html",
  "scripts": {
    "start": "live-server --port=8000 --host=localhost --open=/login.html",
    "dev": "live-server --port=8000 --host=localhost --open=/login.html",
    "serve": "http-server -p 8000 -o login.html",
    "watch": "live-server --port=8000 --watch=css,js,*.html"
  },
  "keywords": [
    "employee",
    "punch",
    "tracking",
    "time"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "http-server": "^14.1.1"
  }
}